using HarmonyLib;
using UnityEngine;


namespace RealTimeClanker.HarmonyPatches
{
    internal class HeckPatch
    {/*
        [HarmonyPatch(typeof(GameObject), nameof(Easings.EaseOutElastic))]
        [HarmonyPrefix]
        static void Cos_Prefix(Easings __instance, ref float p)
        {
            p *= 0.25f;
        }*/
    }
}
